---
import "../styles/global.css";

interface Props {
    title?: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{title}</title>

        <!-- SEO -->
        <meta
            name="description"
            content="Project detail page for the portfolio of Mayke Jaspers"
        />

        <!-- OG -->
        <meta property="og:title" content={title} />
        <meta property="og:type" content="article" />
    </head>

    <body>
        <header>
            <slot name="header" />
        </header>

        <main class="project-layout">
            <slot />
        </main>

        <footer class="site-footer">
            <p>
                © {new Date().getFullYear()} Mayke Jaspers — All Rights Reserved
            </p>
        </footer>
    </body>
</html>

<style>
    .project-layout {
        padding: 2.5rem 1.25rem 4rem;
        max-width: 1100px;
        margin: 0 auto;
    }

    .site-footer {
        text-align: center;
        padding: 2rem 0 3rem;
        font-size: 0.8rem;
        letter-spacing: 0.1em;
        opacity: 0.7;
    }
</style>
